# Copy to .env and fill in.

# Anthropic API key — the only network call this app ever makes.
ANTHROPIC_API_KEY=sk-ant-...

# Anthropic API base URL. Override to point at a proxy or compatible endpoint.
ANTHROPIC_BASE_URL=https://api.anthropic.com

# Colon-separated directories that runs may target. File/shell tools are
# jailed to the chosen repo inside one of these. REQUIRED to run workflows.
PROJECT_ROOTS=/Users/you/Dev

# Optional overrides
# DATA_DIR=data
# PORT=8000
